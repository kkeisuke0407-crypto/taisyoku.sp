# 比較記事型LPの雛形（N社比較 → 2社推薦）

`/manekiya-otakaraya/` を分解して雛形化したもの。**どの案件でも同じ形で作れる**ようにしてある。

| | |
|---|---|
| **ひな形** | `templates/hikaku-lp/index.html` + `style.css` |
| **生成** | `npm run new-hikaku -- scripts/hikaku-configs/<slug>.config.mjs` |
| **config例** | `scripts/hikaku-configs/_example.config.mjs` |
| **完成見本** | `public/manekiya-otakaraya/`（このドキュメントの行番号は全部このファイル） |
| **生成先** | `public/<slug>/`（Astroが `public/` をそのまま公開する） |

> LP3 / LP2nd（`docs/新しいLPの作り方.md`）とは**別ジャンル**。
> あちらは「1社訴求の縦長LP（Astro）」、こちらは「N社比較して2社に絞る読み物（素のHTML）」。
> 検索意図が「◯◯ vs ◯◯」「おすすめ比較」「どこがいい」なら**こちら**を使う。

---

## 1. この型が何をやっているのか（設計思想）

売り込みではなく、**読者の判断を1本の線に載せ替える**構成になっている。

```
読者の状態： どこも「業界最高値」で、違いが分からない
      ↓ ①結論を先に出す           → 探す作業を終わらせる
      ↓ ②ランキング・検索順位を無効化 → 既存の判断軸を壊す
      ↓ ③「最終手取り」を唯一の軸に置く → 新しい判断軸を渡す
      ↓ ④N社を並べて調べた量を見せる  → 軸で比べた事実を出す
      ↓ ⑤査定額の計算式を見せる      → 単価比較が無意味だと分かる
      ↓ ⑥「はい/いいえ」1問に落とす   → 自分の答えが確定する
      ↓ ⑦不安を1つずつ消す          → 行動のコストを下げる
到達点： 「エリア内なら主推薦社／それ以外なら副推薦社」を自分で選んだ気になる
```

**この型の効きどころは3つ。ここを外すと、ただのアフィリ記事に落ちる。**

1. **順位を付けない**（1位/イチオシ/ランキング禁止）。「ランキングは信用できない」と本文で言っておいて自分が順位を付けたら、記事全体の信頼が消える。代わりに**役割の違い**（手取り重視／近くで査定）で2社を分ける。
2. **弱みを本物で書く**。`弱み｜正直に3つ書きます` と `利用前に確認したいこと` は飾ると全部崩れる。ここが信頼の担保。
3. **推薦の根拠を「エリア」に置く**。主観の優劣ではなく、読者の住所という**動かせない事実**で分岐させるので反論が起きない。副推薦社は「主推薦が使えない人の受け皿」であって、格下ではない。

---

## 2. セクション早見表（全21ブロック）

上から順。`必須`は抜くと型が壊れるもの。

| # | ブロック | 主要クラス | 役割 | 必須 | 案件ごとに変えるもの |
|:--:|---|---|---|:--:|---|
| – | FV画像 | `kin-fv--visual` | 商材を一目で示す | ✅ | 画像。**文字を画像に焼かない**（差し替えが効かなくなる） |
| ① | FV直下の結論 | `kin-fv-conclusion` | スクロール0で結論を渡す | ✅ | 原則そのまま |
| ② | 2社バナー＋公式CTA | `kin-pick-verdict` `kin-pv-*` | 最短の成果地点 | ✅ | 社名・バナー・ラベル |
| ③ | 筆者の立ち位置 | `kin-opening` | 「なぜこの人の話を聞くか」 | ✅ | 調べた社数・調べた項目（**事実だけ**） |
| ④ | 導入／ランキング否定 | `kin-intro` `kin-scan-list` | 既存の判断軸を壊す | ✅ | 商材ジャンルの検索実態 |
| ⑤ | 相場・売り時 | `kin-fig` `kin-scan-list--decision` | 今動く理由 | – | 相場のない商材なら別の切り口に差し替え |
| ⑥ | **N社比較表** | `kin-cmp9` `cmp9-*` | 調べた量の可視化 | ✅ | 全社ぶんのセル。**各セルに公式ソース1本** |
| ⑦ | なぜこの2社なのか | `kin-valuation` `kin-why` `kin-quote-check` | **記事の心臓** | ✅ | 選定理由4項目＋確認項目4つ |
| ⑧ | キャンペーン | `kin-camp` `#kin-camp-range` | 今日やる理由 | – | 画像・注記・日付タイプ |
| ⑨ | 会社比較（2社） | `kin-banners` `kin-compare` `kin-box` | 規模の話を切り離す | – | 公表値。`kin-box`で「規模≠買取額」と言い切る |
| ⑩ | **選び方の分岐** | `kin-verdict` | 読者が自分で答えを出す | ✅ | 分岐の質問文・各社の理由 |
| ⑪ | エリア・店舗 | `kin-area` `kin-shops` | ⑩の判定材料 | ✅ | 都府県リスト・店舗写真 |
| ⑫ | なぜ差が出るのか | `kin-punch` | 仕組みの説明 | – | 計算式。`kin-punch`は1セクション1つ |
| ⑬ | 副推薦社の実力 | h3×3〜5 + `kin-list--x` | 受け皿側の担保 | ✅ | 強み＋「利用前に確認したいこと」 |
| ⑭ | 主推薦社の実力 | h3×6〜8 + `kin-list--x` | 最長ブロック | ✅ | 強み＋**弱み3つ（本物）** |
| ⑭a | 相場計算機 | `kin-calc` | その場で金額が出る | – | 貴金属系のみ。`calc:false`で外す |
| ⑭b | 第三者検証 | `kin-media` | 自社以外の裏付け | – | 他メディアの検証。**消さない**（過去に消して弱くなった） |
| ⑭c | 口コミ | `kin-quote` `kin-quote--bad` | 生の声 | – | **`--bad`を必ず1本混ぜる** |
| ⑮ | キャンペーン条件 | – | 特典を使える形にする | – | 適用条件・上限・対象外 |
| ⑯ | 損しないコツ5つ | `kin-scan-list--tips` | 広告→情報に変える | ✅ | 推薦社に依存しない一般ノウハウ |
| ⑰ | 悪徳業者の見分け方 | `kin-list--x` | 「帰っていい」と許可を出す | – | 危険信号5つ |
| ⑱ | 税金 | `kin-table` | 売却後の不安 | – | 課税区分。`tax:false`で外す |
| ⑲ | 申込〜現金化の流れ | `kin-step` | 何をされるか分かる | – | 4〜5ステップ |
| ⑳ | FAQ | `kin-faq` | 検索意図の取りこぼし回収 | ✅ | **10問以上**。schemaにも同内容が入る |
| ㉑ | まとめ表 | `kin-table--summary` | 最後の背中押し | ✅ | 状況→おすすめ→理由。**「他社」の行を1つ入れる** |
| – | 免責 | `kin-disclaimer` | 景表法・ステマ規制 | ✅ | **削らない** |
| – | 固定CTA | `kin-fixed-cta` | 常時表示 | ✅ | 電話=赤（成果）／WEB=緑。左右を入れ替えない |

**CTAブロック（`kin-cta-block`）は7箇所**：⑪⑬⑭⑯⑰⑲㉑の直後。見出しだけ変えて同じ形を反復する。

---

## 3. 4つの機構

### 3-1. CV計測

主CVは**アフィリ先への送客クリック**（詳細は `docs/cv-measurement.md`）。

| イベント | 意味 | 送信先 |
|---|---|---|
| `gold_kaitori_affiliate_click` | 主推薦社（成果） | GA4 + Google Ads |
| `gold_kaitori_other_click` | 副推薦社（非成果） | GA4 + Google Ads |
| `gold_kaitori_phone_click` / `_web_click` | 電話/WEB別の質 | GA4のみ（`send_to`） |
| `gold_kaitori_landing_intent` / `<intent>_open` | 到達 | GA4 |

判定は**href のホスト名**。`secondary.host`（例 `otakaraya.jp`）に一致すれば非成果、それ以外は成果。

> ⚠️ **CTAのclassを増やしたら、スクリプト内の `SEL` にも必ず足す。**
> 過去にクラス名を `kin-pv` → `kin-pv-banner` に変えて `SEL` を直し忘れ、
> FV直下のCTAが**まるごと計測漏れ**していた。雛形にはコメントで注意書きを入れてある。
> 追加したら §6 のチェックリストでクリック実測すること。

### 3-2. キャンペーン日付の自動計算

`#kin-camp-range` と `#kin-today` をJSTで毎日書き換える。`primary.campaign.type` で選ぶ。

- `monthly` … 当月1日〜末日（**まねきや**）
- `weekly` … 次の日曜まで（**おたからや**）

**この2つを取り違えないこと。** 比較表・カウントダウン両方に影響する。

### 3-3. 相場計算機

`/rates.json`（`src/pages/rates.json.js` → `src/data/rates.json`、田中貴金属ベースでGitHub Actionsが毎日更新）を fetch して `単価 × 重量` を出す。貴金属以外の商材では `calc: false`。

### 3-4. 強調の3色

| クラス | 用途 |
|---|---|
| `em-mark`（黄マーカー） | そのセクションの**結論・パンチライン**。最も目立つので1ブロック1〜2個まで |
| `em-green` | メリット・無料・得 |
| `em-red` | 注意・条件・危険信号 |

素の `<strong>`（金の細線）は通常の強調。使い分けないと全部同じ強さに見えてメリハリが消える。

---

## 4. 作り方（手順）

1. **画像を用意**して `public/<slug>/images/` に置く（FV・バナー2枚・ロゴ2枚・地図・店舗写真・キャンペーン画像）。
   FVに**文字を焼き込まない**。結論はHTML側で出す。
2. **config を作る**：`scripts/hikaku-configs/_example.config.mjs` をコピーして `<slug>.config.mjs`。
3. **生成**：
   ```bash
   npm run new-hikaku -- scripts/hikaku-configs/<slug>.config.mjs
   ```
   → `public/<slug>/index.html` と `style.css` ができる。未配置画像・`bad`口コミ無し等は警告で出る。
4. **`▼書き換え` を全部つぶす**。生成直後は雛形の骨が残っている。放置すると別商材の記述が混ざる。
5. `npm run build` → `npm run dev` で `/<slug>/` を目視確認。
6. §6 のチェックリストを通す。
7. ブランチにコミット → PR → マージで公開（デプロイ自動）。

**config の文字列は HTML として埋め込まれる**ので `<strong class="em-mark">` 等がそのまま使える。`alt` / `aria-label` に入る値だけは自動エスケープされる。

---

## 5. 文章の書き方

**→ 詳細は `docs/比較記事LPの文章と強調の型.md`（中身を書く前に必読）**

構成は案件で変わってよいが、**見出しの立て方・文のリズム・強調の付け方は変えない**。そこが揃っていれば別商材でも同じ記事に見える。要点だけ：

- 1文**30字** / 1段落**77字・2〜3文**（150字超えたら割る）
- `em-mark`は**文まるごと**に付け、**1セクション1〜2個**が上限。`em-green`は数字入りの名詞句、`em-red`は否定文・リスク語
- 素の`<strong>`が地の強調（3色の11倍使う）
- h2は「前置き｜核」「読者の問いをそのまま」「まず、〜しました」のいずれか。h3は`強み①｜内容`で内容は動詞で終える
- 口語マーカー（`んです` `正直` `……` `ですよね` `べつに`）は1記事で計20回程度
- **攻めるのは表現、ゆるめるのは事実の断定ではない**。読者の行動の効果は断言でき、店の優劣は断言できない（景表法）

過去に「主推薦社=直営／他社=FC」という論を立てたが、**主推薦社自身がフランチャイザーだった**ため全面的に書き直した。比較の軸にする事実は必ず両社の公式ページで裏を取る。取れない軸は使わない。

## 6. 公開前チェックリスト

**構造**
- [ ] 順位表現（1位／ランキング／イチオシ）が1つも無い
- [ ] `弱み｜正直に3つ書きます` が本物の弱みになっている
- [ ] `kin-quote--bad` が1本以上ある
- [ ] まとめ表に「他社」の行がある
- [ ] `kin-disclaimer` が7項目残っている
- [ ] `▼書き換え` が0件（`grep -c '▼書き換え' public/<slug>/index.html`）

**計測**
- [ ] `SEL` に全CTAクラスが入っている
- [ ] 実際にクリックして `gold_kaitori_affiliate_click` / `gold_kaitori_other_click` が発火する（DevToolsで `dataLayer` を見る）
- [ ] `landingIntent` が既存ページと重複していない
- [ ] 主推薦社のリンクが `rel="sponsored nofollow noopener noreferrer"`

**表示（モバイル優先）**
- [ ] 390px / 360px / 320px で横スクロールが出ない（比較表の `kin-cmp9-wrap` 内だけはOK）
- [ ] 比較表に「👉 横にスクロール」の案内がある
- [ ] キャンペーン日付が今日基準で正しい（`monthly`/`weekly` の取り違え）
- [ ] 固定CTAが本文最下部と重なっていない

**文章（`docs/比較記事LPの文章と強調の型.md`）**
- [ ] `em-mark` が1セクション2個以内（`grep -o 'em-mark' | wc -l` が h2数の1.5倍以内）
- [ ] `em-green` が数字入りの句、`em-red` が否定文・リスク語になっている
- [ ] 推薦社の弱みにも `em-red` を使っている（赤が他社叩き専用になっていない）
- [ ] CTA見出し7箇所が全部違う文
- [ ] h3の連番表記が統一されている（丸数字）
- [ ] 「〜と考えられます」「〜な可能性があります」「いかがでしょうか」が0件
- [ ] 150字を超える段落がない

**中身**
- [ ] 比較表の各セルに公式ソースへのリンクがある
- [ ] 「最大◯%UP」等の特典に、対象外品・上限の注記が付いている
- [ ] FAQが10問以上あり、head の FAQPage schema と一致している
- [ ] 買取実績・第三者検証に「特定の取引例」注記が付いている

---

## 7. 雛形を直したとき

- ひな形（`templates/hikaku-lp/`）を直しても、**既存ページには自動で反映されない**。
  全ページに効かせたい変更なら、各 `public/<slug>/` にも展開する。
- 逆に、実ページで見つけた良い改善は**ひな形に戻す**。戻さないと次の案件で消える。
- ひな形に新しい `{{TOKEN}}` を足したら、`scripts/new-hikaku.mjs` の `tokens` にも足す。
  足し忘れると生成時に「未置換のトークンが残っています」で止まる（安全側に倒してある）。

## 8. 雛形化のときに直した負債

完成見本には残っているが、雛形には持ち込んでいないもの。**実ページを触るときも直す方向で。**

1. **比較表の列3本が後付けJS**（見本 `index.html:203-292`）。`children[5]` のインデックス依存で、列を1本足すだけで壊れる。雛形はHTMLに直書き。
2. **死んだマークアップ**（見本 `index.html:294-312`）。`kin-cmp9-scale` テーブルが inline CSS で `display:none` のまま配信されている。雛形では削除。
3. **head のSEO欠落**（見本 `index.html:3-7`）。`description` / `canonical` / OG / FAQPage schema が無い。雛形では全部入れてある。
4. **inline `<style>` の散らばり**。雛形では `style.css` 末尾の「雛形で共通化した分」に集約し、生成ページは inline `<style>` を持たない。
