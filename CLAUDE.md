# このリポジトリでの作業メモ（Claude 向け）

退職給付金（雇用保険の基本手当／健康保険の傷病手当金）まわりのアフィリエイトLP。
Astro + GitHub Pages。公開ドメインは **taisyoku.hakobu-family.com**。

`gold-kaitori-site`（貴金属買取）から分離した。分けた理由は、案件のNG媒体条項に
「関係のないジャンルのメディア」があり、買取ドメインに退職LPを置くと提携審査で
落ちるリスクがあるため。**このリポジトリに買取系のページを混ぜないこと。**

## LPの作り方

**比較記事型LPの雛形**を使う。デザインシステム（`kin-*` クラス、強調3色）と
CV計測がここに入っている。

- ひな形：`templates/hikaku-lp/index.html` + `style.css`
- 生成　：`npm run new-hikaku -- scripts/hikaku-configs/<slug>.config.mjs` → `public/<slug>/`
- 構成・機構・公開前チェック：`docs/比較記事LPの雛形.md`
- **見出し・強調・語り口調の型：`docs/比較記事LPの文章と強調の型.md`** ← 構成よりこっちが本体

雛形は「N社比較 → 2社推薦」の型を持つが、**内容の型は案件に合わせて外してよい**。
デザインとマークアップ規約（`.entry-content` 配下、`em-mark`/`em-green`/`em-red` と
素の `strong` の使い分け）を揃えることが目的。逸脱したときはコメントで理由を残す。

## 現在のページ

| パス | 狙い | 状態 |
|---|---|---|
| `/taishoku-kyufukin/` | 潜在層（制度を知らない人）向け。PPC想定。金額訴求→CTA→LINE3ステップ→CTA | 主力 |
| `/shobyo-taishokubi/` | 顕在層（比較検討層）向け。4窓口の比較記事 | 入口の作り直しが未着手 |

## このジャンルで外してはいけないこと

案件レギュレーションが「法令順守・虚偽表現」で、**記事確認・CR確認が必須**（戻し目安3営業日）。
国民生活センターが2025年12月3日に注意喚起を出している領域で、名指しされているのは次の3つ。

1. **受給額や受給期間を「増やせる」と掲げる**
2. **オンライン診療などで診断を取らせる**（診断の取得を促す表現）
3. **「最短◯日で受け取れる」**と示唆する

一方、**制度上の上限を条件付きで示すこと自体は名指しの対象ではない**。
競合は全員が金額訴求（最大28ヶ月／最大250万円／200〜400万円）をしているので、
金額では降りない。ただし必ず「条件を満たした場合の制度上の上限」を同一視野に置く。

その他の必須ルール：
- 「誰でももらえる」「必ずもらえる」は書かない
- **申請するのは本人**。退職リリーフは書類準備と進め方の支援であり、申請代行はしない。
  「代わりにやってもらえる」と読める表現はNG（公式ページの注記と矛盾する）
- 労働組合なので団体交渉は可。法律事務の代理は書かない（弁護士法72条）
- サポート範囲として確認できないものを、受け取れるお金として並べない
  （再就職手当を一度入れて外した経緯がある）

## FVの設計ルール（実測にもとづく）

`public/taishoku-kyufukin/index.html` の冒頭コメントにも書いてある。崩さないこと。

1. 1行目は制度ではなく**読者の状態**を名指しする
2. 「退職後」で始めない。在職中・休職中の人を弾く
3. **CTAボタンの上端を画面上から400px以内**に置く（390×844の実効可視は約638px）
4. FVに `em-red` を使わない。療養中の読者に煽りは離脱要因
5. 注記はCTAより上に1行だけ
6. 固定CTAバーは、FVのCTAが画面内にある間は出さない（被って主CTAが隠れる）

## デプロイ

push→`main` で `.github/workflows/deploy.yml` がビルドしてGitHub Pagesへ公開。
PRはマージすると自動デプロイ。Pagesが一時的に `Deployment failed` を返すことが
あるため、deployステップに自動リトライ（最大2回）を入れてある。

## 作業スタイル

- コミット/プッシュは作業ブランチで行い、PR作成→squashマージ。**mainへ直接pushしない**
- 大きめの変更・型からの逸脱・中身の方針が分かれる点は、勝手に進めず一言確認する
